{
  "name": "firestore-rest-api-functions",
  "version": "0.1.0",
  "description": "Cloud Functions for Firestore REST API Extension",
  "main": "index.js",
  "engines": {
    "node": "18"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions,firestore,hosting",
    "deploy": "firebase deploy --only functions,hosting"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.0",
    "firebase-admin": "^11.0.0",
    "firebase-functions": "^4.0.0"
  }
}
